# IEclub平台管理员系统设计文档

**版本**: v1.0  
**设计日期**: 2025-11-05  
**产品经理**: AI Product Manager  
**状态**: 详细设计阶段

---

## 📋 目录

1. [系统概述](#系统概述)
2. [角色与权限设计](#角色与权限设计)
3. [功能模块详解](#功能模块详解)
4. [数据库设计](#数据库设计)
5. [API接口设计](#api接口设计)
6. [前端页面设计](#前端页面设计)
7. [安全机制](#安全机制)
8. [使用场景](#使用场景)

---

## 1. 系统概述

### 1.1 业务目标

IEclub平台管理员系统是一个强大的后台管理工具，旨在为平台管理员提供：
- 🎯 **全局控制**: 统一管理平台所有内容和用户
- 📊 **数据洞察**: 实时监控平台运营数据
- 🔐 **安全保障**: 多层权限控制和操作审计
- 📢 **高效沟通**: 快速发布全平台公告
- 👥 **用户管理**: 精细化的用户权限管理

### 1.2 核心价值

| 价值点 | 说明 | 受益方 |
|-------|------|--------|
| 提升运营效率 | 一站式管理所有平台内容 | 管理员 |
| 保障平台安全 | 实时监控异常行为和内容 | 全体用户 |
| 数据驱动决策 | 可视化数据分析和报表 | 决策层 |
| 快速响应问题 | 及时处理用户投诉和违规内容 | 用户支持团队 |

### 1.3 技术架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端层 (Frontend)                      │
├──────────────────────┬──────────────────────────────────┤
│   Web管理后台         │      小程序管理端                  │
│   (React + Ant Design) │   (WeChat Mini Program)         │
└──────────────────────┴──────────────────────────────────┘
                          ↓ HTTPS/WSS
┌─────────────────────────────────────────────────────────┐
│                    API层 (Backend)                        │
├─────────────────────────────────────────────────────────┤
│  管理员认证中间件 │ 权限控制 │ 审计日志 │ 数据统计        │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                  数据层 (Database)                        │
├─────────────────────────────────────────────────────────┤
│   Admin Table │ Announcement │ AuditLog │ Statistics     │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 角色与权限设计

### 2.1 管理员角色层级

```
超级管理员 (Super Admin)
    ├── 完全控制权限
    ├── 管理其他管理员
    └── 系统配置权限

平台管理员 (Platform Admin)
    ├── 内容管理权限
    ├── 用户管理权限
    └── 数据查看权限

内容审核员 (Content Moderator)
    ├── 审核帖子
    ├── 处理举报
    └── 发送警告

数据分析员 (Data Analyst)
    ├── 查看统计数据
    ├── 导出报表
    └── 只读权限
```

### 2.2 权限矩阵

| 功能模块 | 超级管理员 | 平台管理员 | 内容审核员 | 数据分析员 |
|---------|----------|-----------|----------|-----------|
| 管理员管理 | ✅ | ❌ | ❌ | ❌ |
| 发布公告 | ✅ | ✅ | ❌ | ❌ |
| 用户管理 | ✅ | ✅ | ⚠️ 限制 | ❌ |
| 内容审核 | ✅ | ✅ | ✅ | ❌ |
| 帖子管理 | ✅ | ✅ | ✅ | ❌ |
| 评论管理 | ✅ | ✅ | ✅ | ❌ |
| 数据统计 | ✅ | ✅ | ⚠️ 限制 | ✅ |
| 系统配置 | ✅ | ❌ | ❌ | ❌ |
| 审计日志 | ✅ | ✅ | ❌ | ❌ |

### 2.3 权限枚举定义

```javascript
const ADMIN_PERMISSIONS = {
  // 管理员管理
  ADMIN_CREATE: 'admin:create',
  ADMIN_READ: 'admin:read',
  ADMIN_UPDATE: 'admin:update',
  ADMIN_DELETE: 'admin:delete',
  
  // 用户管理
  USER_READ: 'user:read',
  USER_UPDATE: 'user:update',
  USER_BAN: 'user:ban',
  USER_DELETE: 'user:delete',
  
  // 内容管理
  POST_READ: 'post:read',
  POST_UPDATE: 'post:update',
  POST_DELETE: 'post:delete',
  POST_FEATURE: 'post:feature',
  
  // 公告管理
  ANNOUNCEMENT_CREATE: 'announcement:create',
  ANNOUNCEMENT_UPDATE: 'announcement:update',
  ANNOUNCEMENT_DELETE: 'announcement:delete',
  
  // 数据访问
  STATS_VIEW: 'stats:view',
  STATS_EXPORT: 'stats:export',
  
  // 系统配置
  SYSTEM_CONFIG: 'system:config',
  AUDIT_LOG_VIEW: 'audit:view'
};

const ADMIN_ROLES = {
  SUPER_ADMIN: {
    name: '超级管理员',
    level: 10,
    permissions: Object.values(ADMIN_PERMISSIONS)
  },
  PLATFORM_ADMIN: {
    name: '平台管理员',
    level: 5,
    permissions: [
      'user:read', 'user:update', 'user:ban',
      'post:read', 'post:update', 'post:delete', 'post:feature',
      'announcement:create', 'announcement:update', 'announcement:delete',
      'stats:view', 'audit:view'
    ]
  },
  CONTENT_MODERATOR: {
    name: '内容审核员',
    level: 3,
    permissions: [
      'user:read',
      'post:read', 'post:update', 'post:delete',
      'stats:view'
    ]
  },
  DATA_ANALYST: {
    name: '数据分析员',
    level: 2,
    permissions: [
      'stats:view', 'stats:export'
    ]
  }
};
```

---

## 3. 功能模块详解

### 3.1 仪表盘 (Dashboard)

#### 3.1.1 核心指标卡片

```
┌─────────────────────────────────────────────────────┐
│  📊 实时数据概览                                       │
├──────────┬──────────┬──────────┬──────────────────┤
│ 总用户数   │ 活跃用户  │ 总帖子数  │ 今日新增         │
│ 12,458   │ 3,247    │ 8,923    │ +247            │
└──────────┴──────────┴──────────┴──────────────────┘
```

**指标定义**:
- **总用户数**: 平台注册用户总数
- **活跃用户**: 近7天内有登录行为的用户
- **总帖子数**: 平台发布的帖子总数（不含已删除）
- **今日新增**: 今日新注册用户 + 新发布帖子

#### 3.1.2 数据趋势图

- **用户增长趋势**: 折线图，展示近30天用户注册趋势
- **帖子发布趋势**: 柱状图，展示每日帖子发布量
- **活跃度热力图**: 显示一周内各时段用户活跃度
- **分类占比**: 饼图，展示不同帖子类型的占比

#### 3.1.3 快捷操作

```
[ 发布公告 ]  [ 审核内容 ]  [ 查看举报 ]  [ 导出数据 ]
```

### 3.2 公告管理 (Announcement Management)

#### 3.2.1 公告类型

| 类型 | 说明 | 优先级 | 展示位置 |
|-----|------|-------|---------|
| 系统维护 | 系统升级、维护通知 | 高 | 顶部横幅 + 弹窗 |
| 功能更新 | 新功能上线通知 | 中 | 顶部横幅 |
| 活动通知 | 平台活动、比赛信息 | 中 | 首页 + 消息中心 |
| 政策公告 | 规则变更、政策更新 | 高 | 弹窗 + 消息中心 |
| 普通通知 | 一般性通知 | 低 | 消息中心 |

#### 3.2.2 公告功能

**创建公告**:
```
标题: [输入公告标题]
类型: [下拉选择: 系统维护/功能更新/活动通知/政策公告/普通通知]
优先级: [高/中/低]
内容: [富文本编辑器]
  - 支持文本格式化
  - 支持插入图片
  - 支持插入链接
目标用户: [全体用户/特定角色/特定学校]
生效时间: [立即/定时发布]
过期时间: [永久/指定日期]
展示方式: [弹窗/横幅/仅消息中心]
```

**公告管理列表**:
```
┌────┬──────────┬────────┬────────┬────────┬──────────┬────────┐
│ ID │ 标题      │ 类型    │ 状态   │ 阅读量  │ 发布时间   │ 操作   │
├────┼──────────┼────────┼────────┼────────┼──────────┼────────┤
│ 1  │ 系统维护通知│系统维护│进行中  │ 8,234  │2025-11-05│编辑/删除│
│ 2  │ 新功能上线 │功能更新│已结束  │ 12,456 │2025-11-01│查看/删除│
└────┴──────────┴────────┴────────┴────────┴──────────┴────────┘
```

#### 3.2.3 公告数据统计

- 总阅读量
- 阅读率（阅读人数/目标用户数）
- 点击详情次数
- 关闭/忽略次数

### 3.3 用户管理 (User Management)

#### 3.3.1 用户列表

**搜索和筛选**:
```
搜索: [用户名/邮箱/学号] 🔍
筛选: [全部用户 ▼] [全部学校 ▼] [全部状态 ▼] [注册时间 ▼]
排序: [注册时间降序 ▼]
```

**用户列表表格**:
```
┌────┬────────┬──────────────┬────────┬────────┬────────┬──────────┐
│ ID │ 用户名  │ 邮箱          │ 学校   │ 状态   │ 注册时间│ 操作     │
├────┼────────┼──────────────┼────────┼────────┼────────┼──────────┤
│ 1  │ 张三   │zhang@edu.cn  │ 清华   │ 正常   │11-01   │详情/管理 │
│ 2  │ 李四   │li@edu.cn     │ 北大   │ 警告   │11-02   │详情/管理 │
│ 3  │ 王五   │wang@edu.cn   │ 复旦   │ 封禁   │11-03   │详情/解封 │
└────┴────────┴──────────────┴────────┴────────┴────────┴──────────┘
```

#### 3.3.2 用户详情页

```
┌─────────────────────────────────────────────────────┐
│  👤 用户详细信息                                       │
├─────────────────────────────────────────────────────┤
│  基本信息:                                            │
│    用户ID: 12345                                     │
│    用户名: 张三                                       │
│    邮箱: zhang@edu.cn                                │
│    学校: 清华大学                                     │
│    注册时间: 2025-10-01 14:30:22                     │
│    最后登录: 2025-11-05 09:15:43                     │
│                                                       │
│  统计数据:                                            │
│    发帖数: 23   评论数: 156   点赞数: 89             │
│    被举报次数: 0   警告次数: 0                        │
│                                                       │
│  账户状态:                                            │
│    当前状态: [正常 ✓]                                │
│    角色: 普通用户                                     │
│                                                       │
│  操作记录: (最近10条)                                 │
│    • 2025-11-05 09:15 - 登录系统                     │
│    • 2025-11-05 09:20 - 发布帖子 "寻找前端开发"       │
│    • 2025-11-05 10:30 - 评论了帖子 #8923             │
│                                                       │
│  [ 编辑信息 ] [ 发送警告 ] [ 封禁用户 ] [ 删除账户 ]   │
└─────────────────────────────────────────────────────┘
```

#### 3.3.3 用户管理操作

**1. 发送警告**
```
警告原因: [违反社区规范/发布不当内容/其他]
警告内容: [详细说明违规行为]
警告级别: [轻微/严重/最后警告]
[ 确认发送 ] [ 取消 ]
```

**2. 封禁用户**
```
封禁原因: [必填，说明封禁理由]
封禁时长: [永久/1天/3天/7天/30天]
是否通知用户: [是/否]
[ 确认封禁 ] [ 取消 ]
```

**3. 用户验证**
```
学校认证: [未认证/已认证]
邮箱认证: [未认证/已认证]
[ 手动认证 ] [ 取消认证 ]
```

### 3.4 内容管理 (Content Management)

#### 3.4.1 帖子管理

**帖子列表**:
```
┌────┬──────────┬────────┬────────┬────────┬──────────┬────────┐
│ ID │ 标题      │ 作者   │ 分类   │ 状态   │ 发布时间   │ 操作   │
├────┼──────────┼────────┼────────┼────────┼──────────┼────────┤
│ 1  │找前端开发 │张三    │找队友  │正常    │11-05 9:20│查看/管理│
│ 2  │算法竞赛组队│李四   │比赛    │待审核  │11-05 10:30│审核    │
│ 3  │[违规内容] │王五    │其他    │已隐藏  │11-05 11:00│查看/删除│
└────┴──────────┴────────┴────────┴────────┴──────────┴────────┘
```

**帖子操作**:
- ✅ **置顶**: 将优质帖子置顶显示
- ⭐ **精选**: 标记为精选内容
- 👁️ **隐藏**: 隐藏不当内容（不删除）
- 🗑️ **删除**: 彻底删除违规内容
- 🔒 **锁定**: 锁定帖子，禁止评论
- 📝 **编辑**: 修正帖子内容

#### 3.4.2 举报处理

**举报队列**:
```
┌────┬──────────┬────────┬────────┬──────────┬────────┬────────┐
│ ID │ 被举报内容│举报人  │ 原因   │ 举报时间  │ 状态   │ 操作   │
├────┼──────────┼────────┼────────┼──────────┼────────┼────────┤
│ 1  │帖子#8923 │李四    │不当内容│11-05 10:00│待处理  │处理    │
│ 2  │评论#4512 │王五    │辱骂    │11-05 11:30│处理中  │查看    │
└────┴──────────┴────────┴────────┴──────────┴────────┴────────┘
```

**举报处理流程**:
```
1. 查看举报详情
   ├── 举报内容
   ├── 举报原因
   └── 举报人信息

2. 审核举报内容
   ├── 查看原始内容
   ├── 判断是否违规
   └── 决定处理方式

3. 执行处理操作
   ├── 驳回举报（内容正常）
   ├── 隐藏内容
   ├── 删除内容
   ├── 警告作者
   └── 封禁作者

4. 记录处理结果
   └── 自动通知举报人和内容作者
```

### 3.5 数据统计 (Statistics & Analytics)

#### 3.5.1 用户数据

**用户增长分析**:
- 总用户数
- 新增用户（日/周/月）
- 活跃用户（日/周/月）
- 用户留存率
- 学校分布

**用户行为分析**:
- 平均每日登录次数
- 平均会话时长
- 页面访问热力图
- 功能使用率

#### 3.5.2 内容数据

**帖子统计**:
- 总帖子数
- 发帖趋势
- 分类分布
- 平均互动率

**互动统计**:
- 总评论数
- 总点赞数
- 热门帖子 TOP 10
- 热门作者 TOP 10

#### 3.5.3 数据可视化

```
┌─────────────────────────────────────────────────────┐
│  📈 用户增长趋势 (近30天)                              │
│                                                       │
│   500│                                        ●       │
│   400│                              ●──●──●           │
│   300│                    ●──●──●                     │
│   200│          ●──●──●                               │
│   100│    ●──●                                        │
│      └────────────────────────────────────────       │
│      11/1  11/7  11/14  11/21  11/28  11/5          │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  📊 帖子分类占比                                       │
│                                                       │
│         找队友 35% ████████                           │
│         比赛 25%   ██████                             │
│         分享 20%   █████                              │
│         提问 15%   ████                               │
│         其他 5%    ██                                 │
└─────────────────────────────────────────────────────┘
```

#### 3.5.4 数据导出

**导出选项**:
- 格式: CSV / Excel / PDF
- 时间范围: 自定义日期区间
- 数据类型: 用户数据 / 帖子数据 / 互动数据
- 是否包含详细信息

### 3.6 系统设置 (System Settings)

#### 3.6.1 平台配置

```
平台名称: IEclub
平台口号: 连接创新者，共创未来
平台Logo: [上传]
联系邮箱: admin@ieclub.com
客服电话: 400-xxx-xxxx
```

#### 3.6.2 功能开关

```
☑ 允许用户注册
☑ 需要邮箱验证
☑ 需要学校认证
☑ 允许发布帖子
☑ 允许评论
☐ 维护模式（开启后前端显示维护页面）
```

#### 3.6.3 内容审核设置

```
自动审核:
  ☑ 敏感词过滤
  ☑ 图片识别
  ☐ 需要人工审核所有帖子
  
敏感词库: [编辑词库]
审核通过时间: [自动/24小时内]
```

### 3.7 审计日志 (Audit Logs)

#### 3.7.1 日志记录

**记录内容**:
- 操作人: 管理员ID和用户名
- 操作时间: 精确到秒
- 操作类型: 创建/读取/更新/删除
- 操作对象: 用户/帖子/公告/系统配置
- 操作详情: 具体的修改内容
- IP地址: 操作来源IP
- 操作结果: 成功/失败

**日志级别**:
- 🔴 Critical: 系统配置修改、管理员删除
- 🟠 Warning: 用户封禁、内容删除
- 🟢 Info: 日常操作、数据查看
- 🔵 Debug: 详细调试信息（可关闭）

#### 3.7.2 日志查询

```
时间范围: [2025-11-01] 至 [2025-11-05]
操作人: [全部管理员 ▼]
操作类型: [全部操作 ▼]
日志级别: [全部级别 ▼]
关键词: [搜索操作详情]

[ 查询 ] [ 导出日志 ] [ 清空筛选 ]
```

**日志列表**:
```
┌──────────────────┬────────┬────────┬──────────┬────────┐
│ 时间             │ 管理员 │ 操作   │ 对象     │ 结果   │
├──────────────────┼────────┼────────┼──────────┼────────┤
│11-05 10:30:45   │admin01 │删除    │帖子#8923 │成功    │
│11-05 10:25:12   │admin02 │封禁    │用户#4512 │成功    │
│11-05 10:20:33   │admin01 │发布    │公告#12   │成功    │
└──────────────────┴────────┴────────┴──────────┴────────┘
```

---

## 4. 数据库设计

### 4.1 管理员表 (Admin)

```prisma
model Admin {
  id            Int       @id @default(autoincrement())
  username      String    @unique @db.VarChar(50)
  email         String    @unique @db.VarChar(100)
  password      String    @db.VarChar(255)  // bcrypt加密
  role          String    @db.VarChar(50)   // super_admin, platform_admin, content_moderator, data_analyst
  permissions   Json      // 权限列表数组
  status        String    @default("active") // active, suspended, deleted
  lastLoginAt   DateTime?
  lastLoginIp   String?   @db.VarChar(45)
  
  // 个人信息
  realName      String?   @db.VarChar(50)
  phone         String?   @db.VarChar(20)
  avatar        String?   @db.VarChar(500)
  
  // 安全设置
  twoFactorEnabled Boolean @default(false)
  twoFactorSecret  String? @db.VarChar(255)
  
  // 元数据
  createdAt     DateTime  @default(now())
  createdBy     Int?      // 创建此管理员的管理员ID
  updatedAt     DateTime  @updatedAt
  
  // 关系
  auditLogs     AuditLog[]
  announcements Announcement[]
  creator       Admin?    @relation("AdminCreator", fields: [createdBy], references: [id])
  createdAdmins Admin[]   @relation("AdminCreator")
  
  @@map("admins")
}
```

### 4.2 公告表 (Announcement)

```prisma
model Announcement {
  id            Int       @id @default(autoincrement())
  title         String    @db.VarChar(200)
  content       String    @db.Text
  type          String    @db.VarChar(50)  // system, feature, activity, policy, general
  priority      String    @default("medium") // high, medium, low
  status        String    @default("draft")  // draft, published, expired, deleted
  
  // 展示设置
  displayType   String    @default("notice") // popup, banner, notice
  targetAudience Json     // 目标用户群体配置
  
  // 时间控制
  publishAt     DateTime?  // 定时发布
  expireAt      DateTime?  // 过期时间
  
  // 统计数据
  viewCount     Int       @default(0)
  clickCount    Int       @default(0)
  closeCount    Int       @default(0)
  
  // 元数据
  createdAt     DateTime  @default(now())
  createdBy     Int
  updatedAt     DateTime  @updatedAt
  
  // 关系
  creator       Admin     @relation(fields: [createdBy], references: [id])
  views         AnnouncementView[]
  
  @@map("announcements")
}

model AnnouncementView {
  id              Int       @id @default(autoincrement())
  announcementId  Int
  userId          Int
  viewedAt        DateTime  @default(now())
  clicked         Boolean   @default(false)
  clickedAt       DateTime?
  
  announcement    Announcement @relation(fields: [announcementId], references: [id], onDelete: Cascade)
  user            User         @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@unique([announcementId, userId])
  @@map("announcement_views")
}
```

### 4.3 审计日志表 (AuditLog)

```prisma
model AuditLog {
  id            Int       @id @default(autoincrement())
  adminId       Int
  action        String    @db.VarChar(100)  // create, read, update, delete, ban, unban
  resourceType  String    @db.VarChar(50)   // user, post, comment, announcement, admin, system
  resourceId    Int?      // 被操作对象的ID
  details       Json?     // 详细信息，包含before/after数据
  
  // 请求信息
  ipAddress     String    @db.VarChar(45)
  userAgent     String?   @db.VarChar(500)
  
  // 结果
  status        String    @db.VarChar(20)   // success, failed
  errorMessage  String?   @db.Text
  
  // 元数据
  createdAt     DateTime  @default(now())
  
  // 关系
  admin         Admin     @relation(fields: [adminId], references: [id])
  
  @@index([adminId])
  @@index([resourceType, resourceId])
  @@index([createdAt])
  @@map("audit_logs")
}
```

### 4.4 用户警告表 (UserWarning)

```prisma
model UserWarning {
  id            Int       @id @default(autoincrement())
  userId        Int
  adminId       Int
  reason        String    @db.VarChar(200)
  content       String    @db.Text
  level         String    @db.VarChar(20)   // minor, serious, final
  relatedPostId Int?      // 相关帖子ID
  
  // 状态
  isRead        Boolean   @default(false)
  readAt        DateTime?
  
  // 元数据
  createdAt     DateTime  @default(now())
  
  // 关系
  user          User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@index([userId])
  @@map("user_warnings")
}
```

### 4.5 用户封禁表 (UserBan)

```prisma
model UserBan {
  id            Int       @id @default(autoincrement())
  userId        Int
  adminId       Int
  reason        String    @db.Text
  duration      Int?      // 封禁天数，null表示永久
  
  // 状态
  status        String    @default("active") // active, lifted, expired
  liftedAt      DateTime?
  liftedBy      Int?
  liftReason    String?   @db.Text
  
  // 时间
  startAt       DateTime  @default(now())
  expireAt      DateTime? // 解封时间
  createdAt     DateTime  @default(now())
  
  // 关系
  user          User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@index([userId, status])
  @@map("user_bans")
}
```

### 4.6 数据统计表 (PlatformStats)

```prisma
model PlatformStats {
  id                Int       @id @default(autoincrement())
  date              DateTime  @db.Date
  
  // 用户统计
  totalUsers        Int       @default(0)
  newUsers          Int       @default(0)
  activeUsers       Int       @default(0)
  
  // 内容统计
  totalPosts        Int       @default(0)
  newPosts          Int       @default(0)
  totalComments     Int       @default(0)
  newComments       Int       @default(0)
  
  // 互动统计
  totalLikes        Int       @default(0)
  totalViews        Int       @default(0)
  
  // 其他统计
  totalReports      Int       @default(0)
  processedReports  Int       @default(0)
  
  createdAt         DateTime  @default(now())
  
  @@unique([date])
  @@map("platform_stats")
}
```

---

## 5. API接口设计

### 5.1 管理员认证

#### POST /api/admin/login
管理员登录

**请求体**:
```json
{
  "email": "admin@ieclub.com",
  "password": "AdminPassword123!",
  "twoFactorCode": "123456" // 可选，启用2FA时必填
}
```

**响应**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "admin": {
      "id": 1,
      "username": "admin",
      "email": "admin@ieclub.com",
      "role": "super_admin",
      "permissions": ["admin:create", "user:read", ...],
      "avatar": "https://..."
    }
  }
}
```

#### POST /api/admin/logout
管理员登出

#### GET /api/admin/me
获取当前管理员信息

### 5.2 公告管理

#### GET /api/admin/announcements
获取公告列表

**查询参数**:
- `page`: 页码
- `limit`: 每页数量
- `type`: 公告类型
- `status`: 公告状态
- `keyword`: 搜索关键词

#### POST /api/admin/announcements
创建公告

**请求体**:
```json
{
  "title": "系统维护通知",
  "content": "我们将在2025年11月10日进行系统维护...",
  "type": "system",
  "priority": "high",
  "displayType": "popup",
  "targetAudience": {
    "type": "all" // all, role, school
  },
  "publishAt": "2025-11-10T00:00:00Z",
  "expireAt": "2025-11-11T00:00:00Z"
}
```

#### PUT /api/admin/announcements/:id
更新公告

#### DELETE /api/admin/announcements/:id
删除公告

#### GET /api/admin/announcements/:id/stats
获取公告统计数据

### 5.3 用户管理

#### GET /api/admin/users
获取用户列表

**查询参数**:
- `page`: 页码
- `limit`: 每页数量
- `keyword`: 搜索关键词（用户名/邮箱）
- `school`: 学校筛选
- `status`: 状态筛选
- `sortBy`: 排序字段
- `order`: 排序方向

#### GET /api/admin/users/:id
获取用户详情

**响应**:
```json
{
  "code": 200,
  "data": {
    "user": {
      "id": 1,
      "username": "张三",
      "email": "zhang@edu.cn",
      "school": "清华大学",
      "status": "normal",
      "stats": {
        "postCount": 23,
        "commentCount": 156,
        "likeCount": 89,
        "reportCount": 0,
        "warningCount": 0
      },
      "createdAt": "2025-10-01T14:30:22Z",
      "lastLoginAt": "2025-11-05T09:15:43Z"
    },
    "recentActivities": [...],
    "warnings": [...],
    "bans": [...]
  }
}
```

#### PUT /api/admin/users/:id
更新用户信息

#### POST /api/admin/users/:id/warn
警告用户

**请求体**:
```json
{
  "reason": "违反社区规范",
  "content": "您发布的帖子包含不当内容...",
  "level": "serious",
  "relatedPostId": 8923
}
```

#### POST /api/admin/users/:id/ban
封禁用户

**请求体**:
```json
{
  "reason": "多次发布违规内容",
  "duration": 7, // 天数，null表示永久
  "notifyUser": true
}
```

#### POST /api/admin/users/:id/unban
解封用户

#### DELETE /api/admin/users/:id
删除用户（软删除）

### 5.4 内容管理

#### GET /api/admin/posts
获取帖子列表

**查询参数**:
- `page`, `limit`
- `status`: 状态筛选（all, normal, hidden, deleted, pending）
- `category`: 分类筛选
- `keyword`: 搜索关键词
- `authorId`: 作者ID

#### GET /api/admin/posts/:id
获取帖子详情

#### PUT /api/admin/posts/:id
更新帖子

#### DELETE /api/admin/posts/:id
删除帖子

#### POST /api/admin/posts/:id/hide
隐藏帖子

#### POST /api/admin/posts/:id/feature
设为精选

#### POST /api/admin/posts/:id/pin
置顶帖子

#### POST /api/admin/posts/:id/lock
锁定帖子（禁止评论）

### 5.5 举报管理

#### GET /api/admin/reports
获取举报列表

**查询参数**:
- `page`, `limit`
- `status`: pending, processing, resolved, rejected
- `type`: post, comment, user

#### GET /api/admin/reports/:id
获取举报详情

#### POST /api/admin/reports/:id/process
处理举报

**请求体**:
```json
{
  "action": "delete_content", // reject, hide_content, delete_content, warn_user, ban_user
  "reason": "处理说明",
  "additionalAction": {
    "warnUser": true,
    "warningLevel": "serious"
  }
}
```

### 5.6 数据统计

#### GET /api/admin/stats/dashboard
获取仪表盘数据

**响应**:
```json
{
  "code": 200,
  "data": {
    "overview": {
      "totalUsers": 12458,
      "activeUsers": 3247,
      "totalPosts": 8923,
      "todayNew": {
        "users": 127,
        "posts": 120
      }
    },
    "trends": {
      "userGrowth": [...],
      "postGrowth": [...]
    },
    "distribution": {
      "postsByCategory": {...},
      "usersBySchool": {...}
    }
  }
}
```

#### GET /api/admin/stats/users
用户数据统计

**查询参数**:
- `startDate`, `endDate`: 时间范围
- `granularity`: day, week, month

#### GET /api/admin/stats/posts
帖子数据统计

#### GET /api/admin/stats/engagement
互动数据统计

#### POST /api/admin/stats/export
导出数据

**请求体**:
```json
{
  "type": "users", // users, posts, engagement, all
  "format": "csv", // csv, excel, pdf
  "startDate": "2025-10-01",
  "endDate": "2025-11-05",
  "includeDetails": true
}
```

### 5.7 系统设置

#### GET /api/admin/settings
获取系统设置

#### PUT /api/admin/settings
更新系统设置

#### GET /api/admin/settings/features
获取功能开关状态

#### PUT /api/admin/settings/features
更新功能开关

### 5.8 审计日志

#### GET /api/admin/audit-logs
获取审计日志列表

**查询参数**:
- `page`, `limit`
- `adminId`: 管理员ID
- `action`: 操作类型
- `resourceType`: 资源类型
- `startDate`, `endDate`: 时间范围

#### GET /api/admin/audit-logs/:id
获取审计日志详情

#### POST /api/admin/audit-logs/export
导出审计日志

### 5.9 管理员管理（仅超级管理员）

#### GET /api/admin/admins
获取管理员列表

#### POST /api/admin/admins
创建管理员

**请求体**:
```json
{
  "username": "newadmin",
  "email": "newadmin@ieclub.com",
  "password": "SecurePassword123!",
  "role": "platform_admin",
  "realName": "李四",
  "phone": "13800138000"
}
```

#### PUT /api/admin/admins/:id
更新管理员信息

#### DELETE /api/admin/admins/:id
删除管理员

#### POST /api/admin/admins/:id/suspend
暂停管理员

#### POST /api/admin/admins/:id/activate
激活管理员

---

## 6. 前端页面设计

### 6.1 Web管理后台

#### 6.1.1 技术栈

- **框架**: React 18
- **UI组件库**: Ant Design 5
- **路由**: React Router v6
- **状态管理**: Redux Toolkit + RTK Query
- **图表**: Apache ECharts / Recharts
- **富文本编辑器**: Quill / TinyMCE
- **请求库**: Axios
- **构建工具**: Vite

#### 6.1.2 页面结构

```
/admin
  ├── /login               - 登录页
  ├── /dashboard           - 仪表盘
  ├── /announcements       - 公告管理
  │   ├── /list           - 公告列表
  │   ├── /create         - 创建公告
  │   └── /edit/:id       - 编辑公告
  ├── /users               - 用户管理
  │   ├── /list           - 用户列表
  │   └── /detail/:id     - 用户详情
  ├── /posts               - 帖子管理
  │   ├── /list           - 帖子列表
  │   └── /detail/:id     - 帖子详情
  ├── /reports             - 举报管理
  │   ├── /pending        - 待处理举报
  │   └── /detail/:id     - 举报详情
  ├── /stats               - 数据统计
  │   ├── /overview       - 数据概览
  │   ├── /users          - 用户分析
  │   └── /engagement     - 互动分析
  ├── /audit-logs          - 审计日志
  ├── /settings            - 系统设置
  │   ├── /general        - 基本设置
  │   ├── /features       - 功能开关
  │   └── /moderation     - 审核设置
  └── /admins              - 管理员管理（超管）
      ├── /list           - 管理员列表
      └── /create         - 创建管理员
```

#### 6.1.3 布局设计

```
┌────────────────────────────────────────────────────────┐
│  IEclub 管理后台           🔔  👤 Admin  ⚙️  🚪         │
├──────────┬─────────────────────────────────────────────┤
│          │                                              │
│  📊 仪表盘 │   [当前页面内容区域]                          │
│  📢 公告   │                                              │
│  👥 用户   │                                              │
│  📝 帖子   │                                              │
│  ⚠️  举报   │                                              │
│  📈 统计   │                                              │
│  📋 日志   │                                              │
│  ⚙️  设置   │                                              │
│  👔 管理员 │                                              │
│          │                                              │
└──────────┴─────────────────────────────────────────────┘
```

#### 6.1.4 响应式设计

- **桌面端**: 完整功能，侧边栏固定
- **平板端**: 侧边栏可折叠，保留核心功能
- **移动端**: 底部导航栏，精简功能

### 6.2 小程序管理端

#### 6.2.1 页面结构

```
pages/admin/
  ├── login/              - 管理员登录
  ├── dashboard/          - 仪表盘
  ├── announcements/      - 公告管理
  │   ├── list/          - 公告列表
  │   └── create/        - 发布公告
  ├── users/              - 用户管理
  │   └── list/          - 用户列表（简化）
  ├── posts/              - 帖子管理
  │   └── list/          - 帖子列表
  ├── reports/            - 举报处理
  │   └── list/          - 举报列表
  └── stats/              - 数据统计（简化）
      └── overview/      - 数据概览
```

#### 6.2.2 功能适配

**完整功能**（移植到小程序）:
- ✅ 查看仪表盘数据
- ✅ 发布和管理公告
- ✅ 查看用户列表
- ✅ 审核和管理帖子
- ✅ 处理举报
- ✅ 查看基本统计数据

**限制功能**（仅Web可用）:
- ❌ 复杂数据导出
- ❌ 批量操作
- ❌ 详细审计日志
- ❌ 系统设置修改
- ❌ 管理员管理

#### 6.2.3 小程序界面示例

```
┌─────────────────────────┐
│  IEclub 管理端           │
├─────────────────────────┤
│                          │
│  📊 今日数据              │
│  ┌──────┬──────┬──────┐ │
│  │ 新增  │ 活跃 │ 帖子 │ │
│  │ 127  │ 3247│ 120  │ │
│  └──────┴──────┴──────┘ │
│                          │
│  📢 快捷操作              │
│  [ 发布公告 ]  [ 审核内容 ]│
│  [ 查看举报 ]  [ 用户管理 ]│
│                          │
│  📝 待处理事项            │
│  • 3条待审核帖子          │
│  • 5条待处理举报          │
│  • 2条新用户反馈          │
│                          │
└─────────────────────────┘
   [仪表盘] [内容] [我的]
```

---

## 7. 安全机制

### 7.1 身份认证

#### 7.1.1 密码安全

- **加密方式**: bcrypt，成本因子12
- **密码要求**: 最少8位，包含大小写字母、数字、特殊字符
- **密码过期**: 90天强制修改（可配置）
- **历史密码**: 不允许使用最近5次的密码

#### 7.1.2 双因素认证（2FA）

- **支持方式**: TOTP（Google Authenticator等）
- **强制级别**: 超级管理员强制开启，其他管理员可选
- **备用码**: 生成10个备用码用于紧急登录

#### 7.1.3 会话管理

- **Token类型**: JWT
- **过期时间**: 
  - Access Token: 2小时
  - Refresh Token: 7天
- **自动续期**: 支持无感刷新
- **单点登录**: 可配置是否允许多设备同时登录
- **强制登出**: 管理员可被强制登出所有会话

### 7.2 权限控制

#### 7.2.1 前端权限

```javascript
// 路由守卫
<Route 
  path="/admin/admins" 
  element={
    <RequireAuth permissions={['admin:read']}>
      <AdminList />
    </RequireAuth>
  } 
/>

// 组件级权限
<PermissionButton 
  permissions={['user:ban']}
  onClick={handleBan}
>
  封禁用户
</PermissionButton>
```

#### 7.2.2 后端权限

```javascript
// 中间件验证
router.delete(
  '/api/admin/users/:id',
  authenticateAdmin,
  requirePermissions(['user:delete']),
  deleteUser
);

// 资源级权限
// 低级别管理员不能修改高级别管理员
if (targetAdmin.level >= currentAdmin.level) {
  throw new ForbiddenError('权限不足');
}
```

### 7.3 操作审计

#### 7.3.1 审计范围

**必须记录的操作**:
- 所有管理员登录/登出
- 用户封禁/解封
- 内容删除/隐藏
- 公告发布/删除
- 管理员创建/删除
- 系统配置修改
- 权限变更

**记录内容**:
```javascript
{
  adminId: 1,
  action: 'ban_user',
  resourceType: 'user',
  resourceId: 4512,
  details: {
    before: { status: 'normal' },
    after: { status: 'banned' },
    reason: '多次发布违规内容',
    duration: 7
  },
  ipAddress: '192.168.1.100',
  userAgent: 'Mozilla/5.0...',
  status: 'success',
  createdAt: '2025-11-05T10:30:45Z'
}
```

#### 7.3.2 日志保留

- **保留期限**: 至少1年
- **归档策略**: 超过1年的日志归档到冷存储
- **日志级别**: 
  - Critical: 永久保留
  - Warning: 2年
  - Info: 1年
  - Debug: 30天

### 7.4 数据安全

#### 7.4.1 敏感数据保护

- **密码**: bcrypt加密，不可逆
- **2FA密钥**: AES-256加密存储
- **用户邮箱**: 脱敏显示（zh***@edu.cn）
- **手机号**: 脱敏显示（138****8000）

#### 7.4.2 数据导出限制

- **权限控制**: 需要`stats:export`权限
- **频率限制**: 每小时最多3次
- **数据脱敏**: 导出数据自动脱敏敏感信息
- **水印标记**: PDF导出带管理员水印

### 7.5 API安全

#### 7.5.1 请求限流

```javascript
// IP限流
rateLimit({
  windowMs: 15 * 60 * 1000, // 15分钟
  max: 100 // 最多100次请求
})

// 登录限流
rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 5, // 15分钟内最多5次登录尝试
  skipSuccessfulRequests: true
})
```

#### 7.5.2 请求验证

- **参数验证**: 使用Joi/Yup严格验证
- **SQL注入防护**: 使用Prisma ORM参数化查询
- **XSS防护**: 输入输出过滤
- **CSRF防护**: CSRF Token验证

#### 7.5.3 HTTPS

- **强制HTTPS**: 生产环境强制使用HTTPS
- **HSTS**: 启用HTTP严格传输安全
- **证书**: 使用Let's Encrypt免费证书

---

## 8. 使用场景

### 8.1 日常运营场景

#### 场景1: 发布系统维护公告

**操作流程**:
1. 管理员登录后台
2. 进入"公告管理" → "发布公告"
3. 填写公告信息:
   - 标题: "系统维护通知"
   - 类型: 系统维护
   - 优先级: 高
   - 展示方式: 弹窗
   - 内容: 维护时间和影响范围
4. 设置定时发布（维护前1小时）
5. 点击"发布"
6. 系统自动在指定时间向所有用户推送

**预期效果**:
- 用户打开小程序/网页时看到弹窗提醒
- 公告在消息中心可查看
- 统计显示阅读率

#### 场景2: 处理用户举报

**操作流程**:
1. 收到举报通知（系统推送或主动查看）
2. 进入"举报管理" → "待处理举报"
3. 点击查看举报详情:
   - 举报内容: 帖子#8923
   - 举报原因: 包含不当内容
   - 举报人: 用户#4512
4. 审核被举报内容:
   - 查看帖子原文
   - 判断是否违规
5. 做出处理决定:
   - 如违规: 删除帖子 + 警告作者
   - 如不违规: 驳回举报
6. 填写处理说明
7. 确认处理
8. 系统自动通知举报人和内容作者

**预期效果**:
- 举报状态更新为"已处理"
- 相关用户收到处理结果通知
- 操作记录到审计日志

#### 场景3: 封禁违规用户

**操作流程**:
1. 发现用户多次违规（通过举报或主动巡查）
2. 进入"用户管理" → 搜索用户
3. 点击"查看详情"
4. 查看用户历史:
   - 发帖记录
   - 被举报次数
   - 警告记录
5. 决定封禁，点击"封禁用户"
6. 填写封禁信息:
   - 原因: 多次发布违规内容
   - 时长: 7天
   - 是否通知: 是
7. 确认封禁
8. 系统记录审计日志

**预期效果**:
- 用户被封禁，无法登录
- 用户收到封禁通知
- 可在到期后自动解封

### 8.2 数据分析场景

#### 场景4: 生成月度运营报告

**操作流程**:
1. 进入"数据统计" → "数据概览"
2. 选择时间范围: 上月1日-31日
3. 查看各项数据:
   - 用户增长趋势图
   - 帖子发布统计
   - 活跃度分析
   - 热门内容TOP10
4. 导出数据:
   - 选择导出格式: Excel
   - 包含详细数据
5. 下载报告
6. 整理后向决策层汇报

**预期效果**:
- 获得全面的运营数据
- 识别增长趋势和问题
- 为决策提供数据支持

### 8.3 紧急响应场景

#### 场景5: 处理突发舆情

**操作流程**:
1. 发现敏感内容在平台传播
2. 立即登录管理后台
3. 使用关键词搜索相关帖子
4. 批量隐藏相关内容
5. 发布澄清公告:
   - 类型: 政策公告
   - 优先级: 高
   - 展示方式: 弹窗
   - 立即发布
6. 监控后续情况
7. 记录处理过程到审计日志

**预期效果**:
- 快速控制内容传播
- 及时向用户澄清
- 降低负面影响

---

## 9. 实施计划

### 9.1 开发阶段

| 阶段 | 内容 | 时间 | 负责人 |
|-----|------|------|--------|
| Phase 1 | 数据库设计 + 后端API开发 | Week 1-2 | 后端工程师 |
| Phase 2 | Web管理后台开发 | Week 3-4 | 前端工程师 |
| Phase 3 | 小程序管理端开发 | Week 4-5 | 小程序工程师 |
| Phase 4 | 联调测试 + 安全测试 | Week 6 | 测试工程师 |
| Phase 5 | 部署上线 + 培训 | Week 7 | DevOps |

### 9.2 测试要点

**功能测试**:
- ✅ 所有API接口正常工作
- ✅ 权限控制准确无误
- ✅ 数据统计准确
- ✅ 公告正常发布和展示

**安全测试**:
- ✅ 密码加密存储
- ✅ JWT Token安全
- ✅ SQL注入防护
- ✅ XSS防护
- ✅ CSRF防护
- ✅ 权限绕过测试

**性能测试**:
- ✅ 接口响应时间 < 500ms
- ✅ 大数据量查询优化
- ✅ 并发请求处理

**兼容性测试**:
- ✅ 主流浏览器兼容
- ✅ 响应式布局测试
- ✅ 小程序多版本测试

### 9.3 上线准备

**文档准备**:
- [ ] 管理员使用手册
- [ ] API接口文档
- [ ] 部署文档
- [ ] 应急预案

**培训计划**:
- [ ] 超级管理员培训（2小时）
- [ ] 平台管理员培训（1.5小时）
- [ ] 内容审核员培训（1小时）

**监控告警**:
- [ ] 系统性能监控
- [ ] 错误日志告警
- [ ] 异常操作告警
- [ ] 安全事件告警

---

## 10. 未来扩展

### 10.1 V2.0规划

- 🤖 **AI辅助审核**: 使用机器学习自动识别违规内容
- 📱 **移动端APP**: 独立的管理员APP
- 🌐 **多语言支持**: 国际化准备
- 📊 **高级分析**: 用户画像、行为预测
- 🔔 **智能推送**: 个性化公告推送
- 💬 **在线客服**: 集成客服系统
- 🎨 **白标定制**: 支持多租户

### 10.2 技术优化

- ⚡ **性能优化**: Redis缓存、CDN加速
- 🔄 **实时同步**: WebSocket实时数据更新
- 📦 **微服务化**: 拆分管理员服务
- 🐳 **容器化**: Docker部署
- ☁️ **云原生**: K8s orchestration

---

## 附录

### A. 名词解释

- **超级管理员**: 拥有所有权限的最高级别管理员
- **审计日志**: 记录所有管理员操作的系统日志
- **2FA**: 双因素认证，增强账户安全
- **软删除**: 逻辑删除，数据标记为删除但不从数据库移除
- **RBAC**: 基于角色的访问控制

### B. 联系方式

- **技术支持**: tech@ieclub.com
- **安全问题**: security@ieclub.com
- **紧急联系**: +86 400-xxx-xxxx

---

**文档版本**: v1.0  
**最后更新**: 2025-11-05  
**下次审核**: 2025-12-05

---


