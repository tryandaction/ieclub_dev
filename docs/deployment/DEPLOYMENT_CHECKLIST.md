# 🚀 IEClub 部署检查清单

> 完整的部署前、部署中、部署后检查清单，确保每次部署都安全可靠

---

## 📋 目录

- [测试环境部署清单](#测试环境部署清单)
- [生产环境部署清单](#生产环境部署清单)
- [小程序发布清单](#小程序发布清单)
- [紧急回滚清单](#紧急回滚清单)

---

## 🧪 测试环境部署清单

### 部署前检查 (Pre-Deployment)

```
□ 代码已提交到 Git (develop 分支)
□ 本地开发环境测试通过
□ 没有未解决的 linter 错误
□ 依赖包版本已更新 (package.json)
□ 数据库迁移文件已准备好 (如有新增)
□ 环境变量模板已更新 (.env.staging.template)
□ API 文档已更新 (如有接口变更)
```

### 执行部署

**方式一：标准部署**
```powershell
cd C:\universe\GitHub_try\IEclub_dev
.\scripts\deployment\Deploy-Staging.ps1 -Target all -Message "描述本次更新内容"
```

**方式二：部署并验证（推荐）**
```powershell
cd C:\universe\GitHub_try\IEclub_dev
.\scripts\deployment\Deploy-And-Verify.ps1 -Target all -Message "描述本次更新内容"
```

### 部署后验证 (Post-Deployment)

#### 自动验证 (使用 Deploy-And-Verify.ps1)
```
✓ 网页端访问正常
✓ API 健康检查通过
✓ 登录功能正常
✓ Token 验证正常
✓ 小程序 API 兼容性检查
```

#### 手动验证
```
□ 访问 https://test.ieclub.online
  - 页面正常加载
  - 样式显示正确
  - 图片资源加载

□ 测试核心功能
  - 用户注册
  - 用户登录
  - 数据加载
  - 图片上传
  - 评论互动

□ 检查服务器状态
  ssh root@ieclub.online
  pm2 status                    # 查看进程
  pm2 logs staging-backend      # 查看日志
  
□ 检查数据库
  - 数据库迁移成功
  - 测试数据完整

□ 检查 Redis
  redis-cli ping
  redis-cli info
```

### 问题排查

如果部署失败，按以下顺序检查：

1. **网页端问题**
   ```bash
   ssh root@ieclub.online
   cd /var/www/test.ieclub.online
   ls -la                        # 确认文件存在
   cat index.html | head -20     # 检查内容
   systemctl status nginx        # 检查Nginx状态
   nginx -t                      # 测试配置
   ```

2. **后端问题**
   ```bash
   ssh root@ieclub.online
   cd /root/IEclub_dev_staging/ieclub-backend
   pm2 logs staging-backend --lines 50
   cat .env.staging | grep -v PASSWORD  # 检查配置
   npm list | grep missing              # 检查依赖
   ```

3. **数据库问题**
   ```bash
   mysql -u root -p
   USE ieclub_staging;
   SHOW TABLES;
   SELECT * FROM users LIMIT 1;
   ```

---

## 🚀 生产环境部署清单

### 部署前检查 (Critical - 必须完成所有项)

```
□ 测试环境已充分测试 (至少24小时)
□ 所有测试用例通过
□ 测试环境验证脚本全部通过
□ 代码已 Code Review
□ 性能测试通过 (如有需要)
□ 安全审计通过 (如有敏感功能)
□ 数据库备份已完成
□ 准备好回滚方案
□ 团队成员已知晓部署计划
□ 选择低峰时段部署 (建议: 凌晨或周末)
```

### 数据库准备

```bash
# 连接到生产服务器
ssh root@ieclub.online

# 1. 备份生产数据库
mysqldump -u root -p ieclub > /root/backups/ieclub_$(date +%Y%m%d_%H%M%S).sql

# 2. 验证备份文件
ls -lh /root/backups/
head -50 /root/backups/ieclub_*.sql

# 3. 如果有新的迁移，先在测试环境验证
cd /root/IEclub_dev_staging/ieclub-backend
npx prisma migrate status
```

### 执行部署

**推荐方式：一键部署脚本（包含安全检查）**
```powershell
cd C:\universe\GitHub_try\IEclub_dev
.\scripts\deployment\Deploy-Production-OneClick.ps1 -Target all -Message "v1.0.0 正式发布"
```

**传统方式：手动部署**
```powershell
cd C:\universe\GitHub_try\IEclub_dev
.\scripts\deployment\Deploy-Production.ps1
```

### 部署过程监控

**另开一个终端窗口，实时监控：**
```bash
# 监控后端日志
ssh root@ieclub.online 'pm2 logs ieclub-backend --lines 100'

# 监控服务器资源
ssh root@ieclub.online 'htop'  # 或 'top'

# 监控Nginx访问日志
ssh root@ieclub.online 'tail -f /var/log/nginx/access.log'
```

### 部署后验证 (Critical)

#### 立即验证 (5分钟内)
```
□ 网站可访问: https://ieclub.online
  curl -I https://ieclub.online
  
□ API 健康检查通过
  curl https://ieclub.online/api/health
  
□ PM2 进程正常运行
  ssh root@ieclub.online 'pm2 status'
  
□ 没有明显错误日志
  ssh root@ieclub.online 'pm2 logs ieclub-backend --lines 50 --nostream | grep -i error'
```

#### 功能验证 (15分钟内)
```
□ 用户登录功能
  - 使用测试账号登录
  - 检查 Token 是否正常颁发
  
□ 核心业务功能
  - 浏览帖子列表
  - 发布新帖子
  - 评论互动
  - 图片上传
  
□ 数据完整性
  - 已有数据未丢失
  - 新功能数据正常保存
  
□ 性能检查
  - 页面加载速度 < 3秒
  - API 响应时间 < 500ms
  - 图片加载正常
```

#### 持续监控 (24小时内)
```
□ 错误日志监控
  ssh root@ieclub.online 'pm2 logs ieclub-backend --lines 100' | grep -i error
  
□ 服务器资源
  - CPU 使用率 < 80%
  - 内存使用 < 80%
  - 磁盘空间充足
  
□ 数据库性能
  - 慢查询日志检查
  - 连接池状态正常
  
□ 用户反馈
  - 收集用户报告的问题
  - 监控错误上报系统
```

### 生产环境健康检查脚本

```powershell
# 运行健康检查
cd C:\universe\GitHub_try\IEclub_dev
.\scripts\health-check\Check-Backend-Health.ps1 -Environment production
```

---

## 📱 小程序发布清单

### 发布前准备

```
□ 后端生产环境已部署并稳定运行
□ 小程序代码已切换到生产API地址
□ 合法域名已在微信公众平台配置
  - https://ieclub.online (request)
  - https://ieclub.online (upload)
  - https://ieclub.online (download)
  
□ 小程序基本信息完整
  - 名称、图标、简介
  - 服务类目
  - 所需资质（如有）
  
□ 隐私政策和用户协议已准备
□ 测试账号可用且功能正常
```

### 代码准备

```bash
# 1. 检查小程序目录
cd C:\universe\GitHub_try\IEclub_dev\ieclub-frontend

# 2. 确认 API 配置
cat config/env.js
# 确保 apiBaseUrl = 'https://ieclub.online/api'

# 3. 确认 AppID
cat project.config.json | grep appid

# 4. 清理并构建
npm run clean   # 如果有
npm run build   # 如果有
```

### 在微信开发者工具中检查

```
□ 项目编译无错误
□ Console 无明显错误
□ Network 请求全部成功
□ 核心功能测试通过
  - 登录
  - 浏览内容
  - 发布内容
  - 图片上传
  
□ 真机预览测试通过
  - 扫码在手机上测试
  - 测试各种网络环境 (WiFi/4G/5G)
  - 测试低端和高端设备
  
□ 代码包大小 < 2MB (单包)
□ 总大小 < 16MB
```

### 上传代码

```
在微信开发者工具中:
1. 点击 "上传" 按钮
2. 填写版本信息:
   版本号: 1.0.0 (语义化版本)
   项目备注: 详细描述本次更新内容
3. 上传成功后在微信公众平台查看
```

### 提交审核

```
□ 登录微信公众平台
□ 进入 "版本管理"
□ 找到上传的版本
□ 点击 "提交审核"
□ 填写审核信息:
  - 功能页面路径
  - 测试账号和密码
  - 补充说明
  
□ 勾选同意协议
□ 提交审核
```

### 审核期间监控

```
□ 每天检查审核状态
□ 保持测试账号可用
□ 保持手机畅通（可能电话联系）
□ 准备好补充材料
```

### 审核通过后发布

```
□ 收到审核通过通知
□ 登录微信公众平台
□ 点击 "发布" 按钮
□ 确认发布
□ 等待5-10分钟生效
□ 验证小程序可正常搜索和使用
```

### 发布后验证

```
□ 在微信中搜索到小程序
□ 测试核心功能
□ 检查版本号
□ 监控错误上报
□ 收集用户反馈
```

---

## 🔥 紧急回滚清单

### 何时需要回滚？

- ❌ 生产环境出现严重错误
- ❌ 核心功能无法使用
- ❌ 数据丢失或损坏
- ❌ 性能严重下降
- ❌ 安全漏洞

### 回滚步骤

#### 1. 立即评估

```
□ 确认问题严重程度
□ 估计修复时间
□ 决定是修复还是回滚
□ 通知团队成员
```

#### 2. 回滚后端

```bash
# 连接到服务器
ssh root@ieclub.online

# 查看备份
ls -lh /root/IEclub_dev/ieclub-backend.backup_*

# 回滚到备份
cd /root/IEclub_dev
mv ieclub-backend ieclub-backend.failed_$(date +%Y%m%d_%H%M%S)
cp -r ieclub-backend.backup_YYYYMMDD_HHMMSS ieclub-backend

# 重启服务
cd ieclub-backend
pm2 restart ieclub-backend

# 验证
pm2 logs ieclub-backend --lines 50
curl http://localhost:3000/health
```

#### 3. 回滚数据库（如有迁移）

```bash
# 恢复数据库备份
mysql -u root -p ieclub < /root/backups/ieclub_YYYYMMDD_HHMMSS.sql

# 验证
mysql -u root -p
USE ieclub;
SHOW TABLES;
SELECT COUNT(*) FROM users;
```

#### 4. 回滚前端

```bash
ssh root@ieclub.online

# 查看备份
ls -lh /var/www/ieclub.online.backup_*

# 回滚
mv /var/www/ieclub.online /var/www/ieclub.online.failed_$(date +%Y%m%d_%H%M%S)
cp -r /var/www/ieclub.online.backup_YYYYMMDD_HHMMSS /var/www/ieclub.online

# 重启 Nginx
systemctl reload nginx
```

#### 5. 验证回滚

```
□ 网站可访问
□ API 正常
□ 核心功能恢复
□ 数据完整
□ 监控日志无异常
```

#### 6. 事后分析

```
□ 记录回滚原因
□ 分析问题根源
□ 修复问题
□ 在测试环境重新验证
□ 更新部署流程（如有必要）
```

---

## 📊 检查清单使用建议

### 测试环境 (每次部署)
- ✅ 使用 `Deploy-And-Verify.ps1` 自动验证
- ✅ 至少测试24小时再发布到生产环境

### 生产环境 (重要发布)
- ✅ 完成所有清单项
- ✅ 准备回滚方案
- ✅ 选择低峰时段部署
- ✅ 持续监控24-48小时

### 小程序发布
- ✅ 先发布后端和网页
- ✅ 确保生产环境稳定
- ✅ 准备详细的审核材料
- ✅ 测试账号保持可用

---

## 🔗 相关文档

- [部署指南](./Deployment_guide.md)
- [微信小程序指南](./WECHAT_MINIPROGRAM_GUIDE.md)
- [生产环境部署成功记录](./PRODUCTION_DEPLOYMENT_SUCCESS_2025_11_05.md)
- [关键Bug修复记录](./CRITICAL_BUGS_FIXED_2025_11_05.md)

---

## 📞 紧急联系

如果部署过程中遇到无法解决的问题：

1. **立即停止部署**
2. **不要慌张，按清单检查**
3. **查看日志定位问题**
4. **必要时执行回滚**
5. **联系团队技术负责人**

---

**记住：安全部署，稳定上线！** 🚀

