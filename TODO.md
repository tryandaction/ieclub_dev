# IEClub 待办事项

## 🔥 紧急问题（已解决）
- [x] 登录500错误 - validateRequired is not defined
  - 修复方法：删除不存在的函数调用，使用简单验证
  - 状态：✅ 已修复并部署
  - 提交：d9ef2ac2

## ⚠️ 待修复问题

### authController验证方法（非紧急）
以下方法仍在使用不存在的验证函数，但不影响核心登录功能：

1. **verifyCode** - 验证码验证方法
   - 问题：使用validateRequired, validateEmailFormat, validatePassword
   - 影响：验证码验证API可能出错
   - 优先级：中等

2. **register** - 用户注册方法
   - 问题：使用validateRequired, validatePassword
   - 影响：注册功能可能出错
   - 优先级：高（注册是核心功能）

3. **forgotPassword** - 忘记密码方法
   - 问题：使用validateRequired
   - 影响：密码找回功能可能出错
   - 优先级：中等

4. **resetPassword** - 重置密码方法
   - 问题：使用validateRequired, validatePassword
   - 影响：密码重置功能可能出错
   - 优先级：中等

5. **loginWithCode** - 验证码登录方法
   - 问题：使用validateRequired, validatePassword
   - 影响：验证码登录可能出错
   - 优先级：低（有密码登录作为主要方式）

6. **changePassword** - 修改密码方法
   - 问题：使用validateRequired, validatePassword
   - 影响：修改密码功能可能出错
   - 优先级：中等

### 修复建议
```javascript
// 替换模式：
// 旧代码：
const validation = validateRequired(req.body, ['email', 'password']);
if (!validation.valid) {
  return res.status(400).json({
    success: false,
    message: validation.message
  });
}

// 新代码：
if (!email || !password) {
  return res.status(400).json({
    success: false,
    message: '邮箱和密码不能为空'
  });
}

// 密码长度验证：
// 旧代码：
const passwordCheck = validatePassword(password);
if (!passwordCheck.valid) {
  return res.status(400).json({
    success: false,
    message: passwordCheck.message
  });
}

// 新代码：
if (password.length < 6) {
  return res.status(400).json({
    success: false,
    message: '密码长度不能少于6个字符'
  });
}

// 删除对验证码的validatePassword调用（验证码不需要密码强度验证）
// 删除validateEmailFormat调用（已有checkEmailAllowed）
```

## 📝 开发计划

### 阶段1：稳定核心功能（当前）
- [x] 修复登录500错误
- [ ] 修复注册功能验证问题
- [ ] 测试完整的注册-登录流程
- [ ] 修复其他认证方法的验证问题

### 阶段2：功能开发
- [ ] 完善话题系统
- [ ] 完善评论系统
- [ ] 完善活动系统
- [ ] 完善通知系统

### 阶段3：优化与测试
- [ ] 性能优化
- [ ] 安全加固
- [ ] 单元测试
- [ ] 集成测试

## 💡 注意事项
1. **不要使用自动化脚本批量修复** - Python正则替换容易破坏代码格式
2. **手动逐个修复** - 确保每个修复都经过测试
3. **先修复高优先级问题** - register > 其他方法
4. **每次修复后都要测试** - 确保功能正常
5. **提交前检查语法** - 使用 `node -c` 检查语法错误

## 📊 当前状态
- ✅ 登录功能：正常
- ✅ 服务健康：正常  
- ⚠️ 注册功能：待测试
- ⚠️ 其他认证功能：待修复

最后更新：2025-11-21 14:10
