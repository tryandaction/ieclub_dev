# IEclub 后端日志轮转配置
/var/log/ieclub/*.log {
  daily
  rotate 14
  compress
  delaycompress
  notifempty
  create 0640 www-data www-data
  sharedscripts
  postrotate
    kill -USR1 `cat /var/run/ieclub.pid 2>/dev/null` || true
  endscript
}