# =================================================================
# IEClub 测试环境配置模板
# =================================================================
# 
# 用途: 测试环境的环境变量配置模板
# 使用: 复制此文件为 .env.staging 并填入实际值
# 
# 创建命令:
#   cp env.staging.template .env.staging
#   nano .env.staging  # 编辑配置
#
# 更新: 2025-11-06
# =================================================================

# -----------------------------------------------------------------
# 基础配置
# -----------------------------------------------------------------
NODE_ENV=staging
PORT=3001
API_BASE_URL=https://ieclub.online/api/staging

# -----------------------------------------------------------------
# 数据库配置 (MySQL)
# -----------------------------------------------------------------
# 格式: mysql://用户名:密码@主机:端口/数据库名
DATABASE_URL="mysql://ieclub:your_mysql_password@localhost:3306/ieclub_staging"

# 单独的数据库连接参数（可选，用于脚本）
DB_HOST=localhost
DB_PORT=3306
DB_USER=ieclub
DB_PASSWORD=your_mysql_password
DB_NAME=ieclub_staging

# -----------------------------------------------------------------
# Redis 配置
# -----------------------------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password
REDIS_DB=1

# Session 配置
SESSION_SECRET=your_session_secret_at_least_32_characters_long_staging

# -----------------------------------------------------------------
# JWT 配置
# -----------------------------------------------------------------
# 生成强密钥的方法:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

JWT_SECRET=your_jwt_secret_at_least_64_characters_long_for_staging_environment_security
JWT_REFRESH_SECRET=your_jwt_refresh_secret_at_least_64_characters_long_for_staging

# Token 过期时间
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# -----------------------------------------------------------------
# 邮件服务配置
# -----------------------------------------------------------------
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_specific_password
EMAIL_FROM="IEClub Staging <no-reply@ieclub.online>"

# -----------------------------------------------------------------
# 前端配置
# -----------------------------------------------------------------
FRONTEND_URL=https://ieclub.online
CORS_ORIGIN=https://ieclub.online

# -----------------------------------------------------------------
# 文件上传配置
# -----------------------------------------------------------------
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp

# -----------------------------------------------------------------
# 日志配置
# -----------------------------------------------------------------
LOG_LEVEL=debug
LOG_FILE=./logs/staging.log

# -----------------------------------------------------------------
# 安全配置
# -----------------------------------------------------------------
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# HTTPS 配置
FORCE_HTTPS=true

# -----------------------------------------------------------------
# 第三方服务配置（如需要）
# -----------------------------------------------------------------
# 微信登录
# WECHAT_APP_ID=your_wechat_app_id
# WECHAT_APP_SECRET=your_wechat_app_secret

# 支付宝
# ALIPAY_APP_ID=your_alipay_app_id
# ALIPAY_PRIVATE_KEY=your_alipay_private_key

# 云存储（如七牛、阿里云OSS）
# OSS_ACCESS_KEY_ID=your_oss_access_key
# OSS_ACCESS_KEY_SECRET=your_oss_secret_key
# OSS_BUCKET=your_bucket_name
# OSS_REGION=your_region

# -----------------------------------------------------------------
# 测试环境特殊配置
# -----------------------------------------------------------------
# 启用调试模式
DEBUG=true

# 允许测试账号
ALLOW_TEST_ACCOUNTS=true

# 数据库查询日志
PRISMA_QUERY_LOG=true

# =================================================================
# 重要提示
# =================================================================
#
# 1. ⚠️  不要将此文件的实际配置版本提交到 Git
# 2. 🔒 所有密钥和密码必须使用强随机值
# 3. 📝 生产环境使用 .env 文件，测试环境使用 .env.staging
# 4. 🗄️  测试数据库必须与生产数据库分离
# 5. 📧 测试环境可以使用独立的邮箱账号
#
# =================================================================
